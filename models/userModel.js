var connection = require("./db.js");

/**
 * Retrieve a user from the DB.
 */
module.exports.retrieveUser = function(connection, email, password) {
  //Use connection after the db is set up to retreive user information from the DB.
}